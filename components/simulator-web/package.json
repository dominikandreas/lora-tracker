{
  "name": "lora-tracker-network-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "make wasm",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "serve": "python3 -m http.server 8080 -d app"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
